{
  "name": "fern",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "dev": "bun run --filter docs dev",
    "verify": "node scripts/verify-packages.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
